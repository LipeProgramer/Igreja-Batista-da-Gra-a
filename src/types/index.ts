export type FotoGaleria = {
  src: string;
  legenda: string;
  alt: string;
};

export type Culto = {
  dia: string;
  nome: string;
  descricao: string;
  horario: string;
};

export type NavLink = {
  href: string;
  label: string;
};
